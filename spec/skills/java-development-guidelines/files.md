# 文件处理

文件上传、下载和存储遵循项目既有存储与权限机制，不能把用户输入直接作为服务器文件路径。

- 上传时校验文件大小、类型、扩展名、文件名和业务归属；解析内容前确认格式符合预期。
- 禁止直接使用用户上传的文件名拼接服务器路径，必须防御路径穿越，并使用服务端生成的存储键或项目已有文件服务。
- 不将应用服务器本地磁盘作为业务文件的永久唯一存储；复用项目已有对象存储、文件服务或具备备份与生命周期管理的方案。
- 下载和预览前校验资源归属、租户和数据权限；日志不得泄露本地绝对路径、临时签名或敏感文件内容。
